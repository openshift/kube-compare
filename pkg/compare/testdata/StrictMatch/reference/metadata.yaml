parts:
  - name: ExamplePart
    components:
      - name: Dashboard
        type: Required
        requiredTemplates:
          - path: deploymentDashboard.yaml
          - path: deploymentMetrics.yaml
