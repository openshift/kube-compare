apiVersion: v1
kind: Namespace
metadata:
  name: openshift-storage
  annotations:
{{ merge (or .metadata.annotations (dict))  `workload.openshift.io/allowed: management` | indent 4 }}
  labels:
{{ merge (or .metadata.labels (dict))  `openshift.io/cluster-monitoring: "true"` | indent 4 }}
